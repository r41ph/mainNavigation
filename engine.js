$(function () {
    // Main navigation options
	$('#mainNav-container').mainNav({
        deployFrom: "left",  /* left || right || top */
        stickyNav: true,
        pushNav: true,
        hasOverlay: true,
        overlayColor: "#000",
        overlayOpacity: ".4"
    });

});
